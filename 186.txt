Integers in Python are represented byt the built-in "int" class. The great part is that ints can be of arbitrary length. In the given example, a very long number is first assigned to a variable, and later it is used in some computations, which gives even longer number.

#longnumber #bignumber #integers #bytes #python3 #python #binary #binaryrepresentation #arbitrarylength #longlong