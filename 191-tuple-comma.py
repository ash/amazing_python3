# How to create a tuple with
# only one element

x = ('value') # is it a tuple?
print(type(x)) # it is a string

y = ('value', ) # this comma makes it
# a tuple (because the compiler
# sees that you kind of want more
# elements here)
print(type(y)) # tuple