To create a custom iterator and thus to be able to use "iter" and "next" for an object of a custom class, you need to define the two functions: __iter__ and __next__. In this example, an instance of the "C" class gets an iterator that can count indefinetely.

#python3 #programming #oop #objectorientedprogramming #functionalprogramming #iterators #pythoncode