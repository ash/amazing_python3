# Simple functions can be created
# in just a single line of code.
# Compare:

# Standard function
def add(x, y):
    return x + y

# Lambda function
add1 = lambda x, y: x + y

# Let's use both:
print(add(10, 20))
print(add1(10, 20))
